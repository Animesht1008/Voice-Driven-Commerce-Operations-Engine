{
  "name": "voice-commerce-operations-engine",
  "version": "1.0.0",
  "description": "Voice-Driven Commerce Operations Engine — React + Express + MongoDB/Bolna",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
